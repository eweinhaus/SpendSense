# SpendSense Environment Variables
# Copy this file to .env and fill in your actual values
# The .env file is already in .gitignore and will not be committed

# ============================================================================
# REQUIRED - Application Configuration
# ============================================================================

# OpenAI API Key (Optional - app will use template-based recommendations if not set)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Database Configuration
# Option 1: Use DATABASE_URL (recommended for deployment)
# Format for SQLite: sqlite:///path/to/database.db
# Format for PostgreSQL: postgresql://user:password@host:port/database
DATABASE_URL=sqlite:///spendsense.db

# Option 2: Use DB_PATH (alternative for SQLite, ignored if DATABASE_URL is set)
DB_PATH=spendsense.db

# Debug Mode
# Set to True for development, False for production
DEBUG=False

# ============================================================================
# OPTIONAL - PostgreSQL Configuration (for future migration)
# ============================================================================
# These are only needed if migrating from SQLite to PostgreSQL
# Leave commented if using SQLite

# DB_TYPE=sqlite
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=spendsense
# DB_USER=postgres
# DB_PASSWORD=your_postgres_password_here

# ============================================================================
# OPTIONAL - Development/Testing
# ============================================================================

# Number of users to generate when running generate_data.py
# Default: 75 (range: 50-100)
NUM_USERS=75

# ============================================================================
# DEPLOYMENT - Render.com (Auto-set by Render)
# ============================================================================
# These are automatically set by Render.com, no need to configure locally

# RENDER=true                    # Auto-set by Render
# PYTHONPATH=/opt/render/project/src  # Auto-set by Render

# ============================================================================
# DEPLOYMENT SCRIPT ONLY (Not used by application)
# ============================================================================
# Only needed if running scripts/deploy_render.py
# Get your Render API key from: https://dashboard.render.com/account/api-keys

# RENDER_API_KEY=your_render_api_key_here

